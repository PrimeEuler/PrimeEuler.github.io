name: Build Paper A PDF

on:
  workflow_dispatch:
  push:
    paths:
      - 'unified/discriminant-12-return/foundations/PaperA_ConicTheorem_v2.4.tex'
      - 'unified/discriminant-12-return/figures/paperA_cutting_plane_3panel.py'
      - 'unified/discriminant-12-return/figures/paperA_divisor_summatory_11_3panel.py'
      - 'unified/discriminant-12-return/foundations/build_paper_a.sh'
      - '.github/workflows/build-paper-a.yml'

permissions:
  contents: write

jobs:
  build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-numpy python3-matplotlib cm-super texlive-latex-base texlive-latex-recommended texlive-latex-extra texlive-fonts-recommended

      - name: Build Paper A
        run: bash unified/discriminant-12-return/foundations/build_paper_a.sh

      - name: Commit compiled outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add unified/discriminant-12-return/foundations/PaperA_ConicTheorem_v2.4.pdf
          git add unified/discriminant-12-return/figures/fig_cutting_plane_3panel.pdf
          git add unified/discriminant-12-return/figures/fig_cutting_plane_3panel.png
          git add unified/discriminant-12-return/figures/fig_divisor_summatory_11_3panel.pdf
          git add unified/discriminant-12-return/figures/fig_divisor_summatory_11_3panel.png
          if git diff --cached --quiet; then
            echo "Outputs unchanged"
          else
            git commit -m "Build Paper A v2.4 outputs"
            git pull --rebase origin master
            git push
          fi
