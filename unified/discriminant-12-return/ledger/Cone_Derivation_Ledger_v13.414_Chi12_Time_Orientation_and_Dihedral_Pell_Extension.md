# Cone Derivation Ledger v13.414 — χ12 Time Orientation and the Dihedral Pell Extension

Date: 2026-09-14

Status labels: **[D]** exact derived, **[I]** interpretation, **[Audit]** limitation/guardrail.

## 0. Synchronization note

This checkpoint was produced after re-reading the newest cross-thread ledger/research updates.

- External Audit Round 27 renumbered the Pell/discretization and CRT entries to `v13.411` and `v13.412` and independently verified their boxed identities.
- `v13.410` gives the exact V4/Hadamard decomposition of shell-displacement energy.
- `research-notes/Chi12_Shell_Displacement_Regulator_Cancellation_and_Ideal_Counting_Remainder.md` sharpens the regulator bridge: in the first χ12 shell-displacement moment the linear regulator main terms cancel exactly, leaving the ideal-counting remainder plus a uniformly bounded character-tail correction.

The present entry keeps those updates in force and asks the next operator-level question from `v13.412`: whether the missing V4 labels 5 and 11 arise canonically by adjoining existing involutions to the Pell return.

## 1. Real quadratic conjugation on the ramified ideal [D]

Use the principal paper's ramified basis

\[
f_1=2,\qquad f_2=\sqrt3-1,
\]

so

\[
\alpha=mf_1+nf_2=(2m-n)+n\sqrt3.
\]

Real quadratic conjugation sends

\[
\alpha\mapsto\bar\alpha=(2m-n)-n\sqrt3.
\]

Writing \(\bar\alpha=m'f_1+n'f_2\) gives

\[
n'=-n,\qquad m'=m-n.
\]

Hence its integral matrix in the same basis is

\[
\boxed{
J_f=\begin{pmatrix}1&-1\\0&-1\end{pmatrix}.}
\]

Directly,

\[
\boxed{J_f^2=I.}
\]

For

\[
g_{12}=\begin{pmatrix}3&1\\2&1\end{pmatrix},
\]

one computes

\[
\boxed{
J_f g_{12}J_f
=\begin{pmatrix}1&-1\\-2&3\end{pmatrix}
=g_{12}^{-1}.}
\]

Thus real quadratic conjugation reverses Pell time.

## 2. Factor exchange has the same inversion relation on the Cone [D]

In factor coordinates the Pell/Cone boost is

\[
B_R:(x,y)\mapsto(e^R x,e^{-R}y),
\qquad R=R_{12}=\log(2+\sqrt3).
\]

Factor exchange

\[
F:(x,y)\mapsto(y,x)
\]

satisfies

\[
\boxed{F B_R F=B_{-R}=B_R^{-1}.}
\]

Equivalently, in rapidity coordinates \(s\),

\[
F:s\mapsto-s,
\qquad B_R:s\mapsto s+R.
\]

Therefore factor exchange and real quadratic conjugation are distinct operations on distinct carriers, but they induce the same nontrivial automorphism of the cyclic Pell-return subgroup: inversion.

**[Audit]** They are parallel realizations of the same abstract `C2` action; they are not being identified as the same operator.

## 3. The resulting Pell symmetry is dihedral, not V4 [D]

Modulo 12, `v13.411`–`v13.412` give

\[
g_{12}^{12}=I,
\qquad g_{12}^{6}=7I.
\]

Together with

\[
J_f^2=I,
\qquad J_f g_{12}J_f=g_{12}^{-1},
\]

the subgroup generated by \(g_{12}\) and \(J_f\) is the dihedral extension of the order-12 Pell cycle:

\[
\boxed{
\langle g_{12},J_f\mid g_{12}^{12}=J_f^2=1,\ J_fg_{12}J_f=g_{12}^{-1}\rangle.
}
\]

Since \(J_f\notin\langle g_{12}\rangle\), this has 24 residue operators.

The scalar half-return

\[
g_{12}^{6}=7I
\]

is central, as expected for the half-turn in an even dihedral cycle.

## 4. Why adjoining the involution does not generate 5I or 11I [D]

Every Pell power has determinant

\[
\det(g_{12}^k)=1.
\]

Since

\[
\det J_f=-1,
\]

every reflected element has

\[
\det(g_{12}^kJ_f)=-1\equiv11\pmod{12}.
\]

But for any unit \(r\in U(12)=\{1,5,7,11\}\),

\[
\det(rI)=r^2\equiv1\pmod{12}.
\]

Therefore

\[
\boxed{
g_{12}^kJ_f\ne rI\pmod{12}
\quad\text{for every }r\in U(12).}
\]

Together with `v13.411`'s scalar-intersection result

\[
\langle g_{12}\rangle\cap\{rI:r\in U(12)\}=\{I,7I\},
\]

we conclude

\[
\boxed{
\langle g_{12},J_f\rangle
\cap\{rI:r\in U(12)\}
=\{I,7I\}.}
\]

So the missing scalar matrices \(5I\) and \(11I\) are not recovered by adjoining factor-exchange/real-conjugation inversion.

## 5. Cyclotomic V4 acts on the Pell unit through χ12 [D]

Let

\[
K_{12}=\mathbf Q(\zeta_{12})=\mathbf Q(\sqrt3,i),
\qquad
\lambda=2+\sqrt3.
\]

For \(r\in U(12)\), let

\[
\sigma_r(\zeta_{12})=\zeta_{12}^r.
\]

The four automorphisms act on \((\sqrt3,i)\) as

\[
\begin{array}{c|cc}
r & \sqrt3 & i\\
\hline
1  & +\sqrt3 & +i\\
5  & -\sqrt3 & +i\\
7  & -\sqrt3 & -i\\
11 & +\sqrt3 & -i
\end{array}
\]

Hence on the real Pell unit,

\[
\sigma_r(\lambda)
=2+\sigma_r(\sqrt3).
\]

For the primitive quadratic character modulo 12,

\[
\chi_{12}(1)=\chi_{12}(11)=+1,
\qquad
\chi_{12}(5)=\chi_{12}(7)=-1.
\]

Since

\[
\lambda^{-1}=2-\sqrt3,
\]

we obtain the exact compact formula

\[
\boxed{
\sigma_r(\lambda)=\lambda^{\chi_{12}(r)}.
}
\]

Equivalently,

\[
\boxed{
\begin{array}{c|c}
r & \text{action on Pell time}\\
\hline
1,11 & \lambda\mapsto\lambda\\
5,7 & \lambda\mapsto\lambda^{-1}
\end{array}.}
\]

Thus the Galois V4 acts on the infinite cyclic Pell subgroup through the quotient character

\[
\boxed{
\chi_{12}:U(12)\longrightarrow\{\pm1\}\cong C_2.}
\]

Its kernel is

\[
\boxed{\ker\chi_{12}=\{1,11\},}
\]

and the inversion coset is

\[
\boxed{\{5,7\}.}
\]

## 6. χ12 is therefore a Pell time-orientation character [D/I]

At the operator level, \(\chi_{12}\) has an exact dynamical interpretation:

\[
\chi_{12}(r)=+1
\quad\Longleftrightarrow\quad
\sigma_r\text{ preserves the Pell unit/rapidity orientation},
\]

while

\[
\chi_{12}(r)=-1
\quad\Longleftrightarrow\quad
\sigma_r\text{ reverses it}.
\]

In rapidity language,

\[
R_{12}=\log\lambda,
\]

so formally

\[
\boxed{
\sigma_r(R_{12})=\chi_{12}(r)R_{12}}
\]

when `R12` is understood as the signed logarithmic generator attached to the two real embeddings.

**[I]** This makes \(\chi_{12}\) a natural `time-orientation character` for the Pell/Cone return. The terminology is interpretive; the algebraic statement \(\sigma_r(\lambda)=\lambda^{\chi_{12}(r)}\) is exact.

## 7. Relation to the new χ12 shell-displacement remainder identity [D/I]

The new cross-thread research note proves

\[
E_{12}(n)
=-\mathcal R_{12}(n)-nT_{12}(n),
\qquad |nT_{12}(n)|<2,
\]

where the common regulator main term

\[
L(1,\chi_{12})n
=\frac{R_{12}}{\sqrt3}n
\]

cancels exactly between the reciprocal-character truncation and the ideal-counting truncation.

The present result adds a separate exact role for the **same character**:

\[
\boxed{
\chi_{12}
\text{ simultaneously resolves the shell-displacement channel and the Galois time orientation of }\lambda.}
\]

This is a genuine structural intersection, but the carriers remain different:

- the shell-displacement statistic is built from Euclidean remainders/fractional divisor defects;
- the Pell action is multiplication by the real quadratic unit;
- the common mod-12 character records the sign channel in both settings.

No action of the Pell matrix on the integer divisor lattice is inferred.

## 8. Three distinct appearances of the label 7 [Audit]

The project now contains three mathematically distinct objects carrying the label `7`:

1. the geometric shell \(Y^2=7\) among the four plotted V4 unit shells;
2. the cyclotomic Galois automorphism \(\sigma_7:\zeta_{12}\mapsto\zeta_{12}^7\), which flips both \(\sqrt3\) and \(i\) and therefore inverts \(\lambda\);
3. the scalar residue half-return \(g_{12}^6=7I\pmod{12}\), arising by CRT from \((+1\bmod3,-1\bmod4)\).

These share the unit label `7` but are not the same action. Any future synthesis must specify which carrier is meant.

## 9. Updated structural picture [I]

The exact relationships can now be organized as

\[
U(12)\cong V_4
\xrightarrow{\ \chi_{12}\ }
C_2
\curvearrowright
\langle\lambda\rangle\cong\mathbf Z,
\]

where the nontrivial element of \(C_2\) acts by

\[
\lambda\mapsto\lambda^{-1}.
\]

The same inversion appears geometrically as factor exchange/rapidity reversal.

Independently, reduction of the cyclic Pell return modulo 12 gives

\[
\langle g_{12}\rangle\cong C_{12},
\qquad
g_{12}^{6}=7I,
\]

and adjoining the inversion involution gives a dihedral residue symmetry of order 24.

The V4 Galois symmetry and the Pell dihedral symmetry therefore meet through a **shared inversion quotient** and through the scalar `7` half-return, but neither group should be identified with the other.

## 10. Next exact targets

1. Express the action \(\sigma_r(\lambda)=\lambda^{\chi_{12}(r)}\) directly in the cyclotomic integral basis used for \(\mathfrak P_2\), to compare with the audited literal \(\times\omega\) and transported Frobenius actions after ramified reduction.
2. Determine whether the `χ12 time-orientation` quotient descends nontrivially to any of the finite \(\mathbf F_4\) actions, or whether ramification kills it because direct residue multiplication by \(\lambda\) is identity.
3. Keep checking the ledger for concurrent updates before assigning any stronger meaning to the shared `7` label or the displacement-channel character.

---

**Checkpoint conclusion.** Adjoining real quadratic conjugation/factor exchange does not recover the missing scalar units `5I` and `11I`; instead it exposes the correct structure. The Galois V4 acts on the Pell unit through the quadratic character χ12: residues `1,11` preserve Pell time and `5,7` reverse it. Thus χ12 is an exact sign character for the Pell-return orientation, while the full Pell-plus-inversion symmetry is dihedral. This gives a clean new bridge to the χ12 shell-displacement channel without conflating the two carriers.