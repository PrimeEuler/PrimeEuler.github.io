# Cone Derivation Ledger v13.404 — Odd-Sector Index-2 Certification Target

Date: 2026-09-12

Status labels: **[D]** exact derived, **[N]** numerical, **[V-target]** validation target, **[O]** open.

## 1. Purpose

Continue the independent odd-v parity audit from v13.403 and test the stronger target

\[
\operatorname{ind}_{\le 0}(A_{\rm odd}(a=1))\le 2.
\]

This checkpoint is a certification target, not yet the theorem.

## 2. Split and source-faithful matrix

Use the odd-v/even-index basis with low core

\[
C=\{2,4,\ldots,20\},
\]

finite high block

\[
F=\{22,24,\ldots,4000\},
\]

and tail

\[
T=\{4002,4004,\ldots\}.
\]

The smooth cusp/prime/arch blocks use the source-faithful same-parity formulas verified in v13.403. The odd-sector pole is

\[
P_{\rm pole}^{\rm odd}=2dd^T\succeq0,
\qquad
 d_n=\frac{2k_n\sinh(1/2)}{k_n^2+1/4}.
\]

## 3. Finite high block diagnostic

A fresh source-faithful midpoint assembly gives

\[
\boxed{\lambda_{\min}(A_{FF})\approx 0.533745.}
\]

The validation target will therefore be the deliberately rounded

\[
\boxed{A_{FF}\succeq0.53I.}
\]

This is not promoted in this checkpoint.

## 4. Conservative raw tail floor

For the raw tail beginning at mode 4002, deliberately avoid relying on the sharper localized cusp estimate. Use instead the global component bounds

\[
\|H_{\rm odd}\|\le\pi/2,
\qquad
\|K_{\rm cusp}\|\le0.706,
\qquad
\|B_{\rm prime}\|<2.05,
\]

with the arch tail localization and PSD pole dropped from the lower bound.

This leaves a raw tail floor above approximately 2.58. The point is robustness: the odd sector has enough margin that the global 0.706 cusp remainder bound is sufficient.

## 5. Finite-high / tail cross diagnostic

For the split F={22,...,4000}, T={4002,...}, source-faithful matrix-free diagnostics give:

- adjacent band 4002..16000 cross norm approximately 0.92281;
- explicit remote accumulation through two million raises the full cross norm to approximately 0.97725;
- a leading analytic inverse-power envelope beyond two million contributes approximately 0.03512 at the norm level.

A rounded validation target

\[
\boxed{\|A_{FT}\|<1.013}
\]

is therefore plausible and leaves visible midpoint margin.

Combining the rounded targets

\[
A_{FF}\succeq0.53I,
\qquad
A_{TT}\succeq2.58I,
\qquad
\|A_{FT}\|<1.013,
\]

gives the tail Schur target

\[
A_{TT}-A_{TF}A_{FF}^{-1}A_{FT}
\succeq
\left(2.58-\frac{1.013^2}{0.53}\right)I
>0.64I.
\]

Use the rounded terminal target

\[
\boxed{\delta_{\rm odd}>0.64.}
\]

## 6. Eight-dimensional candidate-positive subspace

The ten-dimensional odd low-core Schur midpoint has two numerical-zero-scale directions, followed by a third small positive level and then rapidly growing positive levels. Therefore freeze the candidate-positive subspace generated by midpoint directions 3 through 10:

\[
W_8=\operatorname{span}\{v_3,\ldots,v_{10}\}.
\]

A midpoint residual-Gram diagnostic for this eight-dimensional subspace gives a normalized full-tail scale around 0.225 after the analytic infinite-tail charge.

Thus the intended terminal inequality has substantial room:

\[
\boxed{\lambda_{\max}(H_{\rm odd})\sim0.225\ll\delta_{\rm odd}>0.64.}
\]

The exact basis will be frozen as dyadic data before any theorem claim, following v13.399-v13.401.

## 7. Consequence if validation succeeds

If an exact frozen rank-8 subspace W_8 is certified positive for the infinite odd-sector Schur complement, then by min-max

\[
\boxed{\operatorname{ind}_{\le0}(S_{10}^{\rm odd})\le2.}
\]

If the high complement is also certified positive, exact Schur-complement inertia transfers this to the full odd sector:

\[
\boxed{\operatorname{ind}_{\le0}(A_{\rm odd}(a=1))\le2.}
\]

Combined with the already certified even-sector bound from v13.402, this would imply a full parity-summed bound of at most six nonpositive directions at a=1. That combined statement is not claimed until the odd validation closes.

## 8. Guardrails

- This checkpoint is a validation target, not an odd-sector theorem.
- The first two odd Schur directions are not called exact kernels.
- The third finite positive level near 1e-10 is not by itself a proof.
- No RH, GRH, exact-zero, or lowest-eigenvalue-zero claim follows.
- The source-faithful post-IBP arch formula is used throughout.

## 9. Next step

Freeze the eight-dimensional odd candidate-positive basis and a fixed preconditioner as exact dyadic inputs, then run outward checks for:

1. A_FF >= 0.53 I;
2. raw tail >= 2.58 I;
3. ||A_FT|| < 1.013;
4. normalized eight-dimensional residual Gram below a conservative ceiling well under 0.64.
