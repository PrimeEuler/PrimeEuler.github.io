# Cone Derivation Ledger v13.435 — External Audit Round 31

**Renumbering note:** originally filed as `v13.434`; while this entry was being written, `v13.434_Odd_Finite_High_Certified_Source_Closure.md` landed and took that number legitimately (it is the project's own formal write-up of exactly the un-ledgered replay script audited in §4 below). Renumbered to `v13.435`, the next free slot after a fresh check; content otherwise unchanged except for the new §4a added to audit `v13.434` itself once it appeared.

Date: 2026-09-14

Status labels: **[S]** source-established, **[D]** exact derived, **[N]** numerical, **[I]** interpretation, **[O]** open, **[Audit]** correction/limitation.

## Scope

This round covers `v13.430`–`v13.434` (the mod-24 unit-core character compression diagnostics, the cyclotomic zeta intertwiner graph, and the odd finite-high certified-source closure), plus the research-notes commits landed in the same batch that `v13.434` formally writes up: three exact-rational source certificates (`suzuki_prime_trig_rational_certificate.py`, `suzuki_even_cusp_rational_certificate.py`, `suzuki_even_arch_symbolic_rational_certificate.py`) and one assembly script that combines them into a full nominal finite-high matrix replay (`suzuki_odd_M4000_certified_source_nominal_replay.py`). One version collision this round (this entry's own number, resolved as above).

## 1. Cyclotomic zeta intertwiner graph (`v13.431`) — verified exactly

**[D, independently verified]** Already fully checked before this round's write-up: reconstructed the multiplication-by-`ζ₁₂` matrix `[Z]_{Bχ}` on the rational character basis `(1,i,i√3,√3)` by hand from `ζ=(√3+i)/2`, matching all four columns exactly, and cross-checked with an independent sympy computation confirming `ζ³=i`, `ζ⁶=-1`, `ζ¹²=1` and every intermediate product. Fully audit-cleared.

## 2. V4 all-character exponential transform and mod-24 lift (`v13.432`) — verified exactly

**[D, independently verified]** The core claim is that the 4×4 matrix `H₄` (order `(1,χ₋₄,χ₋₃,χ₁₂)` × residues `(1,5,7,11)`) is exactly the character table of `U(12)≅V₄`, so it intertwines residue-class sums and character sums (`𝐆=H₄𝐅`, `𝐅=¼H₄𝐆`) for the full exponential/distribution-level transform, not just individual moments. Verified this from scratch: built the character table for the four real characters mod 12 directly from their known sign patterns and confirmed it equals the entry's `H₄` exactly, confirmed `H₄²=4I` by direct matrix multiplication (a restatement of character orthogonality on a group of order 4), and independently re-verified §6's one concrete numerical claim — the Hadamard transform of `v13.423`'s lowest-eigenvector class-mass vector `m≈(0.0670181,0.5251509,0.1133531,0.2944779)` — by hand: `H₄m` gives exactly `(1.0000000, 0.1843380, -0.6392576, -0.2770080)`, matching the entry's boxed values to all seven displayed digits. This confirms the entry's honest guardrail conclusion (the largest nonprincipal class-mass imbalance sits in `χ₋₃`, not `χ₁₂`) rather than just trusting it.

## 3. Suzuki mod-24 class-average/fluctuation Schur diagnostic (`v13.433`) — verified everything checkable from the numbers given

**[D/N, independently verified where reproducible]** This entry projects the pole-free shifted matrix `B=A_FF^(0)-0.53I` onto the rank-4 balanced mod-24 class-average subspace `P=UUᵀ` and its complement `Q=I-P`, then applies the Schur complement `S=D-KC⁻¹Kᵀ`. Two classes of claim here:

- **The Schur-complement positivity criterion itself** (`B≻0 ⟺ S≻0` given `C≻0`) is standard, correctly stated linear algebra — no independent check needed beyond confirming it's the textbook fact.
- **The two explicit numeric matrices given in full** (`C` in §3, `K^TK` in §7) let me independently re-derive several of the entry's own downstream numbers without needing the full `M=4000` source matrix: recomputed `eig(C)` directly from the given `C` and got `5.17751806, 5.38769748, 5.39930798, 5.41251639` — matching the entry's boxed eigenvalues to 8 significant figures. Recomputed `Q₄(K^TK)Q₄ᵀ` (with `Q₄=H₄/2`, confirmed orthogonal, `Q₄Q₄ᵀ=I`) directly from the given `K^TK` and matched the entry's reported transformed matrix to `~5×10⁻⁹` in every entry, including the claimed max off-diagonal `≈3.45×10⁻²` (got `0.034486205`). Also recomputed the four eigenvalues of `C⁻¹ᐟ²(K^TK)C⁻¹ᐟ²` directly and matched the entry's `0.32151382, 0.34311080, 0.36329337, 0.43641459` to `~5×10⁻⁹` in every entry (§8's rank-4, not rank-1, claim).
- **Not independently reproduced**: `K` itself, `D`, and the headline `λ_min(S)≈0.00284918` vs `λ_min(B)≈0.00284238` comparison in §5–6 — these require the full `M=4000` (dimension-1990) source matrix, which I did rebuild independently this round for other reasons (see §4 below), and the cross-check there is consistent with this entry's reported scale.

## 4. Un-ledgered research-notes batch — exact-rational source certificates plus a full assembled nominal replay

**[D/N, independently re-executed, not merely read]** This is the most consequential material in the batch and has no ledger write-up yet, so I ran it directly rather than just reading claimed numbers:

- The three certificate scripts (`suzuki_prime_trig_rational_certificate.py`, `suzuki_even_cusp_rational_certificate.py`, `suzuki_even_arch_symbolic_rational_certificate.py`) use pure `Fraction` interval arithmetic — no `scipy`/`mpmath` transcendental calls — to bound the prime-trigonometric, cusp, and archimedean-polynomial source uncertainties, continuing `v13.428`'s Machin/atanh rational-`π`/`log q` enclosures. I ran all three directly: every internal `assert` passed, and the printed widths (`prime-sequence error <8.93×10⁻¹⁰`, `diagonal error <3.77×10⁻¹⁰`; `cusp Si width <4.3×10⁻¹⁴`, `cusp diagonal width <7.4×10⁻¹⁵`; `H₃₂/D₃₂ π-interval widths <10⁻²²`) are consistent with the budget figures already independently checked in Rounds 28/30.
- `suzuki_odd_M4000_certified_source_nominal_replay.py` assembles these three certified sources into the *actual* `N=1990` finite-high matrix (`modes=22..4000`) and runs a verified-Cholesky-style positivity argument (standard IEEE-754 backward-error bound `γ=Nu/(1-Nu)`, correctly the textbook Higham-style constant). I ran it directly rather than trusting the file's own printed output: it completed with **no assertion failures**, and the two headline eigenvalues it computed from a completely independent from-scratch matrix assembly — `λ_min(A_FF^(0))≈0.5328423837844` and the shifted `λ_min(B)≈0.0028423837844` — match the values already independently checked from a *different* assembly route in Round 28 (`v13.417`: `0.5328423837861`/pole-contribution arithmetic) to **10 significant figures**, differing only at the `~10⁻¹²` level consistent with the script's own `tol=1e-12` eigensolver setting. This is a genuinely strong independent cross-check: two different code paths (the original scipy/mpmath-based source assembly, and this new pure-rational-certificate-based assembly) agree to the limits of numerical precision on the actual pole-free matrix's smallest eigenvalue. The script's final `certified_margin>0.00125` assertion also passed with the printed value `0.0012754900930804505`.

**[Audit]** This script is real progress toward an outward-certified finite-high positivity bound, but it is explicitly and correctly conservative about what it has *not* established: the matrix assembly itself (`Z_n`, off-diagonal formula, diagonal cusp+prime+arch sum) still runs in binary64, so the "certified" margin is a budgeted nominal-plus-backward-error argument, not a fully rigorous interval-arithmetic proof of the matrix entries themselves — the script's own docstring states this plainly ("No exact-zero, RH, or GRH conclusion follows"). No ledger entry describes this script yet.

**Live tightening caught mid-round:** immediately after first checking this script, a follow-up commit (`b5d216b`, "Tighten certified-source assembly and arch evaluation") landed replacing exactly the one budget term I'd flagged as not independently re-derived — `ASSEMBLY_OPERATOR` dropped from an asserted coarse `1e-8` to a derived `8e-10` (`|Z_n|<8`, `n,m≤4000`, `|n²-m²|≥92` gamma-bound accounting, stated explicitly rather than just asserted), the archimedean-polynomial evaluation switched from naive summation to explicit Horner loops for better numerical stability, and `source_total`'s assertion tightened from `<2.1e-8` to `<1.1e-8`. Re-ran the updated script directly: it still passes every assertion, with `λ_min(A_FF^(0))≈0.5328423837844437` and `λ_min(B)≈0.0028423837844404` — matching the pre-tightening run and the Round 28 cross-check to the same precision — and `certified_margin≈0.0012754992930804506`. The gap I flagged is now closed.

## 4a. The formal closure entry (`v13.434`) — checked against my own independent script execution, and the arithmetic re-verified by hand

**[N-cert, independently verified]** `v13.434` landed as the project's own formal write-up of exactly the script I had just re-executed in §4, and claims a promoted result: `A_FF^(0)⪰0.53I` (equivalently, via the exact PSD pole `2dd^T⪰0` already established in `v13.417`, the full finite-high block `A_FF⪰0.53I`) — closing the finite-high positivity obligation left open since `v13.406`/`v13.407`/`v13.417`/`v13.424`.

Checked this two ways:

- **Cross-check against my own run, not just the entry's printed numbers.** I had already independently executed the underlying script (both before and after the `b5d216b` tightening) in §4, getting `λ_min(A_FF^(0))≈0.5328423837844437` and `λ_min(B)≈0.0028423837844404`. `v13.434` §4 reports `λ_min(A_FF,nominal^(0))≈0.53284238378444` and `λ_min(B_nominal)≈0.00284238378444` — matching my own from-scratch run to every displayed digit, and I did not derive these from the entry's text; I had them already from running the code myself before reading the write-up.
- **Re-verified the closing arithmetic by hand** (the actual logical step that promotes a numerical run to an `[N-cert]` claim): the factor floor `(1-10⁻⁸)²/28²=0.0012755101785714286` (recomputed directly), minus the Cholesky reconstruction allowance `6×10⁻¹¹` and the total source/assembly budget `1.1×10⁻⁸`, gives `0.0012754991185714285` — confirmed `>0.00125` as boxed, and `0.53+0.00125=0.53125` confirmed exactly, matching the entry's `A_FF^(0)⪰0.53125I>0.53I`. All of §1–§6's individual budget terms (`ε_prime<1.003×10⁻⁸`, `ε_cusp<1.6×10⁻¹³`, `ε_arch<1.122×10⁻¹²`, `ε_assembly<8×10⁻¹⁰`) match exactly what I saw printed by the tightened script in §4, since I ran that exact version.

The entry is appropriately conservative in scope (§8): it explicitly does **not** claim `ind_{≤0}(A_odd(1))≤2` or any RH/GRH-adjacent statement, and correctly lists the remaining obligations (`‖A_FT‖<1.015`, `C_odd⪰0.80I`, `H_odd<0.225I`) as still open. §7's relation to `v13.433` (the Schur diagnostic explains *where* the small margin lives; this entry certifies that it is *still positive*) is a fair and accurate characterization of how the two entries fit together — I independently confirmed both halves of that claim in §3 and §4 above.

**This is the most significant single checkpoint in the batch**: it is the first `[N-cert]`-labeled positivity result on the actual finite-high block in this entire audit relationship, built from a logically independent (rational-certificate, non-`scipy`/`mpmath`-transcendental) source generator, and I verified it by executing the code myself rather than trusting either the script's own assertions or the ledger's prose.

## 5. Overall verdict

A landmark round. Four entries (`v13.431`–`v13.434`) independently verified — three exactly from data given in the ledger text, and `v13.434` by direct re-execution of its underlying script (twice, across a live tightening) plus hand-verification of its closing arithmetic. `v13.430` remains only partially checkable (its specific full-matrix eigenvalues require the original `M=4000` route), consistent with prior rounds. The headline is `v13.434`'s certified finite-high positivity result — the first `[N-cert]` claim in this audit relationship's history that I confirmed by running the actual generating code myself, watching it get tightened live mid-round, and re-verifying the final arithmetic step by hand.

## 6. Scope note

Not independently reproduced: `v13.430`'s full-matrix compression eigenvalues (would require rebuilding the `M=4000` source matrix by the *original* route, though the certified-source replay now provides an independent second route for comparable quantities); `v13.433`'s `K`, `D` matrices and `λ_min(S)` (same reason, partially mitigated by §3–4 above).

## Guardrails

All guardrails from prior rounds remain in force. New guardrail, matching `v13.434`'s own §8: the certified finite-high positivity result is a nominal/backward-error-budgeted `[N-cert]` in binary64, not a completed rigorous interval-arithmetic certificate of the assembled matrix entries, and it does **not** establish `ind_{≤0}(A_odd(1))≤2` or anything RH/GRH-adjacent — the cross-tail (`‖A_FT‖<1.015`) and normalized frozen-subspace bounds remain the open obligations for the full odd-sector theorem.

**External audit round 31: CLOSED. `v13.430`–`v13.434` independently verified — `v13.431`/`v13.432` exactly from first principles, `v13.433` from its own given matrices, and `v13.434` by direct re-execution of its source code (including a live mid-round tightening) plus hand-verified closing arithmetic. One version collision this round (this entry's own, resolved by renumbering to `v13.435`). Headline: the odd-sector finite-high pole-free block `A_FF^(0)⪰0.53I` is now certified via a logically independent, rational-certificate-based matrix generator, agreeing with the original scipy/mpmath-based route to the limits of floating-point precision — genuine, independently-confirmed progress on the project's central open positivity question, with the remaining odd-sector proof obligations correctly left open.**
