# Cone Derivation Ledger v13.309 — 4V/V4 Fourier Overlay of the Divisor Shell-Defect Field

## Scope

This checkpoint records a local/global unification between:

- the elementary four-vertex factor cell,
- the universal quarter shift \(\delta^2/4\),
- the mod-12 Klein-four character basis generated by 5 and 7,
- the divisor shell-defect field \(\{n/k\}=(n-Y_k^2)/k\), and
- the quotient-block compression of both untwisted and twisted defect sums.

This entry is deliberately isolated from the current Suzuki/coercivity sequence. No RH/GRH, positivity, Fredholm, or spectral claim is made here.

Companion research note:

`research-notes/Factor_Cell_V4_Fourier_Divisor_Defect_Overlay.md`

---

## 1. Four-vertex factor cell as a V4/Walsh carrier

For cell spacing \(\delta\), let

\[
F_{00}=K_-^2=\delta^2pq,
\quad
F_{10}=J_+^2=\delta^2(p+1)q,
\]
\[
F_{01}=J_-^2=\delta^2p(q+1),
\quad
F_{11}=K_+^2=\delta^2(p+1)(q+1).
\]

The factor/divisor lattice is the vertex lattice itself; these are the four consecutive vertices of one elementary factor cell.

Use the normalized V4/Walsh transform

\[
\widehat F_\chi=\frac14\sum_{a,b\in\{0,1\}}\chi(a,b)F_{ab},
\]

with

\[
\chi_0=1,
\quad
\chi_5=(-1)^a,
\quad
\chi_7=(-1)^b,
\quad
\chi_{11}=(-1)^{a+b}.
\]

Direct calculation yields

\[
\boxed{\widehat F_{\chi_0}=Y_c^2},
\]

\[
\boxed{\widehat F_{\chi_5}=-\frac{\delta}{2}v_c},
\qquad
\boxed{\widehat F_{\chi_7}=-\frac{\delta}{2}u_c},
\]

and the mixed/checkerboard coefficient

\[
\boxed{\widehat F_{\chi_{11}}=\frac{\delta^2}{4}=M_F^2}.
\]

Equivalently,

\[
K_-^2+K_+^2-J_+^2-J_-^2=\delta^2.
\]

Thus the universal quarter shift is exactly the mixed V4 Fourier mode of the four squared corner amplitudes.

The center coordinates are recovered by

\[
\boxed{T_c=-\frac{\widehat F_{\chi_5}+\widehat F_{\chi_7}}{\delta}},
\qquad
\boxed{X_c=\frac{\widehat F_{\chi_5}-\widehat F_{\chi_7}}{\delta}}.
\]

So one factor cell decomposes into four exact V4 channels:

\[
\boxed{
\left(
Y_c^2,
-\frac{\delta v_c}{2},
-\frac{\delta u_c}{2},
\frac{\delta^2}{4}
\right)}.
\]

---

## 2. Mod-12 generator basis

The unit group

\[
U(12)=\{1,5,7,11\}\cong V_4
\]

is generated by 5 and 7:

\[
5^2\equiv7^2\equiv1\pmod{12},
\qquad
5\cdot7\equiv11\pmod{12}.
\]

Using

\[
1\leftrightarrow(0,0),\quad
5\leftrightarrow(1,0),\quad
7\leftrightarrow(0,1),\quad
11\leftrightarrow(1,1),
\]

the discriminant-12 mixed character is

\[
\boxed{\chi_{12}=(-1)^{a+b}},
\]

with sign pattern

\[
\boxed{(+,-,-,+)}.
\]

Earlier project work established

\[
\chi_{12}=\chi_{-4}\chi_{-3},
\]

and identified the natural arithmetic V4 as the biquadratic/Galois structure associated with \(\mathbb Q(i,\sqrt3)\), not a period-four Farey cycle.

The labels 1,5,7,11 on the local Boolean square are therefore character-basis labels only. No literal residue assignment to arbitrary factor-cell vertices is asserted.

---

## 3. Divisor shell-defect field

For

\[
q_k=\left\lfloor\frac nk\right\rfloor,
\qquad
r_k=n-kq_k,
\]

the divisor-staircase vertex \((k,q_k)\) has cone height

\[
Y_k^2=kq_k=n-r_k.
\]

Hence

\[
\boxed{
\delta_k(n)
=\left\{\frac nk\right\}
=\frac{r_k}{k}
=\frac{n-Y_k^2}{k}.
}
\]

The trivial-character projection is exactly

\[
\boxed{
E_0(n)=\sum_{k=1}^n\delta_k(n)=nH_n-D(n).
}
\]

The pre-existing discriminant-12 twisted sum is therefore exactly

\[
\boxed{
E_{12}(n)
=\sum_{k\le n}\chi_{12}(k)\delta_k(n)
=\sum_{k\le n}\chi_{12}(k)\frac{n-Y_k^2}{k}.
}
\]

This gives the mod-12 twisted sawtooth a cone-geometric meaning: it is the mixed/checkerboard character projection of the shell-defect field.

The four arithmetic channels are

\[
E_\chi(n)=\sum_{k\le n}\chi(k)\delta_k(n),
\qquad
\chi\in\{1,\chi_{-4},\chi_{-3},\chi_{12}\}.
\]

---

## 4. Exact quotient-block compression for all V4 channels

On a quotient block where

\[
q=\left\lfloor\frac nk\right\rfloor
\]

is constant for \(L_q\le k\le R_q\),

\[
\delta_k(n)=\frac nk-q.
\]

Define

\[
H_\chi(N)=\sum_{k\le N}\frac{\chi(k)}{k},
\qquad
C_\chi(N)=\sum_{k\le N}\chi(k).
\]

Then

\[
\boxed{
E_{\chi,q}(n)
=n\bigl[H_\chi(R_q)-H_\chi(L_q-1)\bigr]
-q\bigl[C_\chi(R_q)-C_\chi(L_q-1)\bigr].
}
\]

Summing over the distinct quotient values,

\[
\boxed{E_\chi(n)=\sum_{q\in Q(n)}E_{\chi,q}(n)}.
\]

Since \(|Q(n)|=O(\sqrt n)\), every V4 character channel inherits the same hyperbola-method compression as the untwisted discrepancy.

For \(\chi=1\), this reduces to the exact block decomposition of

\[
nH_n-D(n).
\]

For \(\chi=\chi_{12}\), it compresses the discriminant-12 twisted shell defect.

---

## 5. Local/global synthesis

The exact shared structure is:

\[
\boxed{
\begin{array}{c|c}
\text{local factor cell} & \text{global divisor staircase}\\
\hline
F_{ab}=Y_{ab}^2 & \delta_k=(n-Y_k^2)/k\\
V_4\text{ on four positions} & V_4\text{ on mod-12 characters}\\
(-1)^{a+b} & \chi_{12}(k)\\
\delta^2/4 & \sum\chi_{12}(k)\delta_k
\end{array}}
\]

Hence

\[
\boxed{
\text{the four-vertex factor cell carries the same V4 character algebra that organizes the mod-12 divisor twist.}
}
\]

This is a representation-theoretic overlay, not an identification of carriers.

---

## 6. Guardrails

- Do not identify arbitrary factor-cell vertices with literal residues \(1,5,7,11\pmod{12}\).
- Do not identify the local factor-cell V4 with a Farey/null-diamond period-four orbit.
- The common object is the V4 character/sign algebra.
- The local \(\delta^2/4\) and the global \(E_{12}(n)\) are different observables in the same mixed character channel.
- The divisor-shell identities, the \(A_n\) decomposition, and prime-contact criterion are unchanged.
- No new primality theorem is claimed.

## Interpretation

The 5/7 generator structure now has a precise place in the divisor-shell framework without becoming a distraction from it. Locally, the V4 transform resolves one elementary factor cell into center product, two centered linear directions, and the universal mixed quarter shift. Globally, the same four-character algebra resolves the divisor shell-defect field into untwisted and mod-12 twisted channels, all with the same O(\sqrt n) quotient-block compression.