# Cone Derivation Ledger v13.480 — M16001 Outward Replay: Fail-Closed Source-Backend Gate

Date: 2026-09-14

Status labels: **[D]** exact derived, **[N]** numerical target, **[N-cert]** previously certified input, **[Audit]** guardrail.

## 0. Synchronization

The live ledger was checked at the beginning of this replay and again immediately before this numbered write. Parallel arithmetic entries advanced through `v13.479`; `v13.480` was free at the final check.

The purpose here is to answer a precise certification question: can the shifted-nominal `M=16001` anisotropic seven-plane target now be promoted from midpoint diagnostic to a theorem proving

\[
\operatorname{ind}_{\le0}(A_{\rm even}(1))\le3?
\]

The answer is **not yet**. The numerical target passes with substantial margin, but the committed executable chain still lacks one source-generation enclosure needed to call the replay outward-certified.

## 1. Certified source-operator input [N-cert]

`v13.357` was written specifically for the shifted pole-free high block

\[
B=A_{0,[21,16001]}-0.22I.
\]

It proves the dimension-free archimedean compression error

\[
\boxed{\|\Delta K_{\rm arch}\|_2\le1.22\times10^{-13}}
\]

and sets the global exact-vs-high-precision-nominal matrix target

\[
\boxed{\|B_{\rm exact}-B_{\rm nominal}\|_2<2\times10^{-13}}.
\]

Prime and cusp constants are to be reconstructed from certified logarithm/trigonometric and rational/series enclosures at precision far exceeding this operator budget.

Thus the shifted-operator architecture

\[
A_{\rm exact}\succeq A_{\rm nominal}-2\times10^{-13}I
\]

is the correct theorem-level abstraction, provided the `A_nominal` used in the M16001 replay is actually generated by the certified high-precision nominal backend.

## 2. Shifted M16001 midpoint target [N]

The current helper

`research-notes/suzuki_M16001_even_index3_shifted_nominal_target.py`

replays the anisotropic split on

\[
A_{\rm nominal}-2\times10^{-13}I.
\]

It records the deliberately rounded midpoint inequalities

\[
\boxed{\lambda_{\min}(A_Q)>0.67,}
\]

\[
\boxed{\|B_{Q,N}\|_2<2.7\times10^{-7},}
\]

\[
\boxed{a_N>9.1\times10^{-13},}
\]

and therefore the six-plane Schur penalty target

\[
\boxed{\frac{\|B_{Q,N}\|_2^2}{\lambda_{\min}(A_Q)}<1.1\times10^{-13}.}
\]

The resulting shifted seven-plane midpoint margin is

\[
\boxed{m_{7,\rm mid}>8.0\times10^{-13}.}
\]

The preceding unshifted anisotropic replay gave a stronger midpoint target of approximately

\[
1.016\times10^{-12}.
\]

## 3. Structured-arithmetic replay [N]

The separate long-double cross-check

`research-notes/suzuki_M16001_shifted_nominal_longdouble_crosscheck.py`

uses the same source scalar payload but reruns the 7991-mode structured LDL solve in `numpy.longdouble`.

It verifies

\[
\boxed{\max|\Delta X|<4\times10^{-15}}
\]

for the finite-solve coefficients and

\[
\boxed{|\Delta(v^TSv)|<2\times10^{-16}}
\]

for the relevant unresolved Rayleigh scalar. The observed Rayleigh change is approximately `1.36e-16`.

Hence structured finite-solve arithmetic is far below the `8e-13` shifted target margin. This is not the active obstacle.

## 4. Requested four interval objects: present status [Audit]

The requested certification objects are:

1. shifted finite Schur block;
2. normalized `Q/N` cross block;
3. remote residual Gram through `2,000,000`;
4. blockwise analytic far tail beyond `2,000,000`.

Their current **midpoint/conditional** targets pass. However, the M16001 helper obtains its finite source scalar payload with ordinary SciPy quadrature and special-function calls. Those calls do not return outward enclosures.

Therefore the presently committed executable chain does **not** supply rigorous intervals for items 1–3. Item 4 has an analytic inequality once its moment coefficients are enclosed, but those coefficients inherit the same uncertified M16001 nominal payload, so it also cannot yet be called fully outward-certified.

## 5. Concrete executable gap [Audit]

The existing exact-rational prime-trigonometric helper

`research-notes/suzuki_prime_trig_rational_certificate.py`

contains

```python
RMAX = 2000
```

which matches the old `M=3999` finite split. The present split requires recurrence propagation through

\[
\boxed{r=8000}
\]

for modes through `16001`.

Thus the immediate executable gate is not a new mathematical estimate; it is extension of the already-established certified scalar-generation architecture from `r<=2000` to `r<=8000`, together with the corresponding cusp/arch high-precision nominal replay at the same split.

A fail-closed helper was committed as

`research-notes/suzuki_M16001_outward_replay_fail_closed_gate.py`

(commit `1e3a93786f8780c86ff4a46971bd02fefeb11993`).

## 6. Interval-margin report [Audit]

The strongest honest report at this checkpoint is:

| quantity | numerical / certified status |
|---|---|
| exact-vs-high-precision-nominal source operator | **certified** `<2e-13` |
| arch component of source operator | **certified** `<=1.22e-13` |
| shifted normalized Q block | midpoint target `>0.67`; outward M16001 scalar backend not yet wired |
| shifted Q/N cross | midpoint target `<2.7e-7`; outward M16001 scalar backend not yet wired |
| unresolved scalar | midpoint target `>9.1e-13`; outward M16001 scalar backend not yet wired |
| six-plane cross penalty | midpoint target `<1.1e-13` |
| shifted seven-plane margin | midpoint target `>8.0e-13` |
| structured solve Rayleigh sensitivity | replayed `<2e-16` |
| structured solve coefficient sensitivity | replayed `<4e-15` |

Because a valid interval for the M16001 nominal source payload is still missing, there is no theorem-level interval lower endpoint for the final seven-plane margin yet. The correct interval statement is therefore **open**, not `>0`.

## 7. Theorem status [Audit]

The outward replay **fails closed** before theorem promotion.

Therefore the certified result remains

\[
\boxed{\operatorname{ind}_{\le0}(A_{\rm even}(1))\le4.}
\]

and, together with the certified odd-sector bound,

\[
\boxed{\operatorname{ind}_{\le0}(A_{a=1})\le6.}
\]

The proposed strengthenings

\[
\operatorname{ind}_{\le0}(A_{\rm even}(1))\le3
\]

and

\[
\operatorname{ind}_{\le0}(A_{a=1})\le5
\]

are **not promoted** here.

## 8. Exact next step

Extend the certified nominal source generator to the full M16001 range:

1. prime trigonometric recurrence certificate from `RMAX=2000` to `RMAX=8000` with outward recurrence-error propagation;
2. cusp `Si/Ci` rational/series reconstruction through mode 16001;
3. archimedean degree-64 polynomial nominal evaluation through mode 16001 using the already-certified `6.1e-14` uniform remainder;
4. feed those outward nominal intervals into the shifted structured LDL and remote Gram replay;
5. form the final 7x7 block lower matrix with directed rounding.

The current midpoint target leaves more than `8e-13` of room, while the observed structured-arithmetic sensitivity is only `~1e-16`, so the route remains numerically plausible. The unresolved issue is executable interval provenance, not a failed margin.

---

**Checkpoint conclusion.** The M16001 shifted anisotropic calculation continues to pass numerically with a seven-plane target above `8e-13`, and the long-double structured solve is stable by several orders of magnitude inside that margin. The dimension-free exact-vs-high-precision-nominal operator enclosure `<2e-13` is already established specifically for the `[21,16001]` block. Nevertheless, the currently committed M16001 replay still builds its nominal source payload from non-interval SciPy calls, while the exact-rational prime recurrence certificate is only wired through `r<=2000`. Consequently the requested outward replay is not yet complete, and the even-sector index bound cannot honestly be promoted from four to three at this checkpoint.