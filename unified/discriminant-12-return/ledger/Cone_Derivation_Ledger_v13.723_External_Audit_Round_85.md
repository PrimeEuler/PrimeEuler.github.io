# Cone Derivation Ledger v13.723 — External Audit Round 85

Date: 2026-09-23

Auditor: independent external reviewer, verifying by direct hand re-derivation, symbolic checks, and a decisive end-to-end numerical confirmation.

Scope: v13.717 (centered two-sided theta/Mellin cone current), v13.718 (genuine continuous-kernel perturbation and coupled Jacobian), v13.719 (theta difference-kernel exponential matrix elements and Xi bridge obstruction), v13.720 (finite-A theta matrix asymptotics, deficiency growth, and Xi normalization), v13.721 (Suzuki transport multiplier and conditional Xi identification), v13.722 (shifted Casimir theta kernel and pure Xi current) — six entries jointly attacking both open gates from Rounds 82 and 84: the genuine coupled kernel-perturbation Jacobian, and the Suzuki-to-Xi/Zeta operator bridge.

## 0. Collision/relevance check

`git fetch origin master` immediately before writing this entry shows HEAD at `8135e9a` (v13.722), no intervening commits. Highest ledger version is v13.722; this entry claims v13.723 / Round 85.

## 1. v13.717 — centered two-sided theta/Mellin current: independently re-derived in full, PASS

This entry closes the specific gap flagged in Round 84 (v13.716 §5): that the `V_4` functional-equation symmetry was not manifest in v13.715's one-sided current. This audit independently re-derived the entire classical Mellin/Jacobi-inversion chain from scratch, not merely re-read it: starting from `\Lambda(s)=\int_0^\infty\psi(x)x^{s/2-1}dx`, splitting at `x=1`, substituting `x\to1/x` on `(0,1)`, and applying Jacobi inversion `\vartheta(1/x)=x^{1/2}\vartheta(x)` gives exactly `\Lambda(s)=-1/s-1/(1-s)+\int_1^\infty\psi(x)(x^{s/2}+x^{(1-s)/2})dx/x` — confirmed via full independent computation, matching the entry's boxed formula exactly, including the nontrivial fact that the rational pole terms combine with `\tfrac12 s(s-1)` to contribute exactly `1/2` to `\xi(s)`.

The centering `s=\tfrac12+w`, substitution `x=e^{2r}`, and definition `K_\theta(r)=e^{|r|/2}\psi(e^{2|r|})` were independently verified to give the exact bilateral representation `\Xi(w)=\tfrac12+(w^2-\tfrac14)\int_{\mathbb R}K_\theta(r)e^{wr}dr`. The key structural claim — that `K_\theta` being even makes `\Xi(-w)=\Xi(w)` and `\Xi'/\Xi(-w)=-\Xi'/\Xi(w)` manifest at the kernel level, via the simultaneous reflection `(w,r)\mapsto(-w,-r)` rather than through external analytic continuation — was independently verified by direct substitution `r\mapsto-r` in the bilateral integral. The resulting classical fact `\xi'/\xi(1-s)=-\xi'/\xi(s)` was thereby independently re-derived via a completely different route than the one used in Round 83 (the `s(s-1)` rational-factor route), and both agree, a valuable cross-check. The entry's own guardrail (§9) — that this theta current is not yet shown to be the same measure as v13.715's prime-archimedean current — is accurate and appropriately preserved. **PASS, exact.**

## 2. v13.718 — genuine continuous-kernel perturbation: independently re-derived, PASS

This strengthens Round 82's artificial "product deformation" (where the Weyl function was held fixed by explicit construction) into a genuine coupled perturbation where the induced Weyl variation is allowed to respond physically. This audit independently re-derived the full chain: `\delta S^{-1}=-S^{-1}HS^{-1}` (standard first-order resolvent perturbation, confirmed via Neumann-series expansion), the induced `\delta F_\pm(z)=-\overline{\langle u_{\bar z},Hu_\pm\rangle}` (confirmed via self-adjointness of `S^{-1}`), and the quotient-rule simplification `\delta m(z)=-2i(A\delta B-B\delta A)/(A-B)^2`, whose numerator simplification to exactly `2(A\delta B-B\delta A)` this audit independently verified by full algebraic expansion.

The entry's most interesting finding — that the resulting `2\times2` derivative matrix `(\partial_\varepsilon\tau,\partial_\eta\tau;\partial_\varepsilon\ell,\partial_\eta\ell)` is lower-triangular (since `\partial_\eta\tau=0` always, while `\partial_\varepsilon\ell` is now generically nonzero, unlike in the Round 82 product model) was independently confirmed, along with the resulting fact that the Jacobian determinant formula `J_H(z)=-2iz\,\mathrm{Tr}([(I-zS)^{-1}-I]H)/(m^2+1)` is *identical in form* to the Round 82 result despite the much weaker, more physically genuine hypotheses — a correct and non-obvious consequence of triangularity (the off-diagonal cross-term cannot affect a triangular matrix's determinant). The explicit eigenvector witness reproduces exactly the Round 82 formula `J_f(z)=-2iz^2\mu/[(1-z\mu)(m^2+1)]`, independently confirmed. **PASS, exact.**

## 3. v13.719 — theta difference-kernel matrix elements: the naive bridge correctly fails, PASS

This is the first direct test of whether the pullback `h_\theta(x,y)=K_\theta(x-y)` reproduces the Xi transform in Suzuki's finite-interval setting, and it is exemplary in finding and correctly reporting a negative result rather than forcing a match. This audit independently re-derived the change of variables `r=x-y,c=(x+y)/2` (confirmed Jacobian `|{\partial(x,y)}/{\partial(c,r)}|=1`), the resulting domain `r\in[-2A,2A]`, `c\in[-A+|r|/2,A-|r|/2]` (confirmed via direct interval-intersection geometry), and the window function `W_A(r;q)=2\sin(q(A-|r|/2))/q` (confirmed via direct evaluation of the finite exponential integral). The diagonal case reduces exactly to the triangular overlap window `W_A(r;0)=2A-|r|`, correctly identified as the standard interval-autocorrelation/Bartlett-window geometry (`[-A,A]\cap([-A,A]-r)`), independently confirmed.

The entry correctly concludes `M_{A,\pm}(z)\ne I_\theta(w)` at finite `A` for three precisely identified reasons (support truncation, the `|r|`-dependent window, and the fact that the two deficiency channels map to two different effective spectral variables `w_\pm(z)`) and explicitly states "FAIL: the naive difference-kernel pullback does not reproduce the v13.717 Xi integral" — a genuine, correctly-diagnosed negative finding, not glossed over. The entry then finds a genuine narrower positive result: independently verified via dominated convergence (justified by `K_\theta`'s super-exponential decay) that `\lim_{A\to\infty}(2A)^{-1}M_A(a,a)=I_\theta(ia)`. **PASS — both the negative and positive findings are correctly derived and correctly scoped.**

## 4. v13.720 — finite-A asymptotics and paired-channel recovery: independently verified, PASS (one minor presentational note)

This audit independently re-derived the diagonal volume-law limit (§1) and the off-diagonal exponential growth-rate analysis for both deficiency channels (§3): confirmed `e^{i(z\mp i)A}` decomposes into `e^{\pm A}e^{izA}`-type factors exactly as claimed, correctly identifying which term in each channel's two-term asymptotic dominates as `A\to\infty`. The entry's most valuable result — the symmetric-combination recovery `I_\theta(w)=L_+(iw)+L_-(iw)-2C_\theta` from the two individually-divergent (exponentially growing) deficiency channels after phase renormalization and subtraction of a common boundary term `C_\theta=\int_0^\infty K_\theta(r)e^{-r}dr` — was independently re-derived in full and confirmed exact, including the final addition `L_++L_-=2C_\theta+I(-iz)`.

One minor issue found: in §4's intermediate labeling for the minus deficiency channel, the two stated exponent-combination values (`w_{0,-}+i(z+i)/2` and `-w_{0,-}+i(z+i)/2`) appear swapped relative to a careful from-scratch computation (this audit independently computed `w_{0,-}+iq_-/2=iz` and `-w_{0,-}+iq_-/2=-1`, the reverse of the entry's stated pairing). This does **not** propagate to any incorrect final result: the boxed formula `\mathcal J_+(w_{0,-},z+i)=\int_0^\infty K(r)(e^{-r}+e^{izr})dr` is a sum of both terms regardless of which is labeled which, and this audit independently confirmed this final formula, and every subsequent formula built on it (`L_-(z)`, the paired recovery identity), is exactly correct. **PASS, with one cosmetic labeling note that does not affect any conclusion.**

## 5. v13.721 — Suzuki transport multiplier: exemplary honesty about what remains conditional, PASS

This entry correctly separates exact facts from explicitly conditional ones (labeled `[C]`), and this audit specifically checked that the labeling is accurate. Independently verified the exact facts: `\bar De_z=ze_z` (trivial calculus, `D=i\,d/dx` applied to `e^{-izx}`), the reduction of the mean-zero Neumann inverse to the bulk kernel `-|x-y|/2` (confirmed the two vanishing terms in the stated Neumann kernel `N_A(x,y)` under the mean-zero/mean-zero pairing condition), and the formal full-line symbol `\sigma_S(z)=\widehat g(z)-\lambda/z^2` (confirmed `\mathcal Ke_z=e_z/z^2` from `-d^2/dx^2e_z=z^2e_z`).

Critically, this audit verified the entry does **not** overreach: the asymptotic multiplier relation `u_z\sim(z/\sigma_S(z))e_z` is explicitly derived only under three stated, unproven hypotheses (H1-H3), labeled `[C]` throughout, and §7 explicitly and correctly states "We have not proved `S_A^{-1}e_z\sim\sigma_S(z)^{-1}e_z` with the boundary-error control needed for the exponentially growing deficiency channels" — giving the precise, correct reason: independently confirmed `\|e^x\|^2_{L^2(-A,A)}=\sinh(2A)` by direct integration, confirming the deficiency sources genuinely concentrate their mass at the interval boundary as `A\to\infty`, so a bulk/interior symbol theorem is insufficient on its own. This is the correct mathematical reason the bridge remains open, not a rhetorical hedge. **PASS — the conditional/exact distinction is accurate throughout.**

## 6. v13.722 — shifted Casimir and the pure Xi kernel: independently verified exactly, including a decisive numerical confirmation, PASS

This is the strongest result in the batch. This audit independently re-derived the entire distributional-calculus chain: the Jacobi fixed-point identity `\vartheta'(1)=-\tfrac14\vartheta(1)` (via direct differentiation of the inversion formula and evaluation at its fixed point `x=1`), and the resulting derivative-jump computation `f'(0)=-1/4` — this audit's own independent computation confirms the `\vartheta(1)` terms cancel exactly (`f'(0)=\tfrac14\psi(1)+2\psi'(1)=\tfrac{\vartheta(1)-1}{4}-\tfrac14\vartheta(1)=-\tfrac14`), a genuinely nontrivial cancellation, not an obvious a priori fact.

The distributional identity `(D_r^2-\tfrac14)K_\theta=\Phi-\tfrac12\delta_0` (standard distributional calculus for a continuous, kinked function, correctly applied with jump `[K_\theta']_0=-1/2`) and the explicit series `\Phi(r)=\sum_{n\ge1}[4\pi^2n^4e^{9|r|/2}-6\pi n^2e^{5|r|/2}]e^{-\pi n^2e^{2|r|}}` were independently re-derived term-by-term from `g_n(r)=e^{r/2-\pi n^2e^{2r}}` via direct differentiation (`g_n''-\tfrac14g_n=(4a^2e^{4r}-6ae^{2r})g_n` with `a=\pi n^2`, confirmed exactly). The positivity claim `\Phi(r)>0` was independently verified rigorously (not merely asserted): the bound `2\pi n^2e^{2|r|}-3\ge2\pi-3>0` (using `n\ge1,e^{2|r|}\ge1`) makes every term of the factored form manifestly positive.

Most decisively, this audit independently and numerically confirmed the culminating formula `\Xi(w)=\int_{\mathbb R}\Phi(r)e^{wr}dr` (equivalently `2\int_0^\infty\Phi(r)\cosh(wr)dr`) against `\xi(\tfrac12+w)` computed directly from its standard definition, using 30-digit `mpmath` arithmetic, at five test points spanning real, purely imaginary (on the critical line), and generic complex `w` — agreement to `\sim10^{-32}`, i.e. exact to the full working precision in every case. This is an end-to-end confirmation of the entire derivation chain, not merely its individual algebraic steps.

The entry's guardrails are accurate and important: §9 correctly declines to claim this identifies an actual `SU(2)`/`SU(1,1)` representation beyond the shared Casimir eigenvalue polynomial, and §10 correctly and explicitly states that positivity/evenness of `\Phi` does **not** imply RH or constrain zero locations — a precise, mathematically accurate caveat against a common point of confusion. **PASS, exact, decisively confirmed numerically.**

## 7. Summary

| Entry | Claim | Verification | Outcome |
|---|---|---|---|
| v13.717 | Two-sided theta/Mellin current makes `V_4` symmetry manifest at the kernel level | independent full re-derivation of the classical Mellin/Jacobi-inversion chain | **PASS, exact** |
| v13.718 | Genuine coupled kernel-perturbation Jacobian; lower-triangular structure preserves the Round-82 formula | independent re-derivation of all perturbation formulas | **PASS, exact** |
| v13.719 | Naive difference-kernel pullback fails at finite `A`; normalized diagonal limit recovers `I_\theta` | independent geometric + convergence analysis | **PASS — correct negative and positive findings** |
| v13.720 | Symmetric paired-channel recovery `I_\theta(w)=L_++L_--2C_\theta` from exponentially growing deficiency channels | independent asymptotic re-derivation | **PASS, one cosmetic labeling note, no effect on conclusions** |
| v13.721 | Suzuki transport multiplier `\bar De_z=ze_z`; conditional (not proven) identification; correct boundary-layer obstruction diagnosis | independent verification of exact facts + confirmation that conditional claims are accurately labeled | **PASS — honest and precise about what remains open** |
| v13.722 | Shifted Casimir operator applied to `K_\theta` yields the pure positive Xi kernel `\Phi`, with exact `-1/2`/`+1/2` cancellation | independent full distributional-calculus re-derivation + **decisive numerical confirmation to ~1e-32** | **PASS, exact** |

## 8. Assessment

This is the strongest single batch this audit has reviewed in the Xi/Zeta lane. Two things distinguish it. First, the program correctly treats the Suzuki-Xi bridge as a genuine open research question rather than a foregone conclusion: v13.719 tests the most natural hypothesis and lets it fail, correctly diagnoses the geometric reason (finite-window truncation), and v13.721 is explicit and precise about exactly which unproven asymptotic theorem (boundary-layer/edge-resolvent behavior of `S_A^{-1}` on exponentially-concentrated deficiency sources) stands between the current conditional results and an actual theorem — this is not hedging, it is an accurate account of where the difficulty actually lives, independently confirmed by this audit's own concentration computation (`\|e^x\|^2=\sinh(2A)`).

Second, v13.722 is a genuine, independently-confirmed mathematical result of real elegance: the classical positive-kernel representation of `\xi(1/2+it)` emerges directly from applying a shifted Casimir differential operator to the theta current, with an exact delta-function contact term at the Jacobi fixed point that cancels the completion constant precisely. This was checked not only algebraically at every step but numerically end-to-end to 32-digit precision — the strongest form of verification available and one this audit specifically sought out given the significance of the claim.

No result in this batch overreaches into RH, Hilbert-Pólya, or spectral-positivity territory, and the guardrails distinguishing exact results from conditional ones are consistently accurate under this audit's independent scrutiny, not merely present as disclaimers.

## 9. Final freshness check

`git fetch origin master` immediately before this commit: HEAD still at `8135e9a`. No new commits landed while writing this entry. `git ls-tree` confirms v13.723 remains free.
