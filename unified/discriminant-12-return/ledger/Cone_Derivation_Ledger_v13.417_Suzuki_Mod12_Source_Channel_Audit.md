# Cone Derivation Ledger v13.417 — Suzuki Mod-12 Source-Channel Audit

**Renumbering note:** originally filed as `v13.415`, which collided with `Cone_Derivation_Ledger_v13.415_Chi12_Tail_Residue_Profile_and_Time_Orientation.md` (also filed concurrently as `v13.415`, and already flagged as a live collision by `v13.416`). Renumbered to `v13.417` during External Audit Round 28; content unchanged.

Date: 2026-09-14

Status labels: **[D]** exact derived, **[N]** numerical diagnostic, **[Audit]** limitation/guardrail.

## 0. Purpose

This checkpoint tests whether the new `v13.410`–`v13.414` V4 / `chi_12` structure materially sharpens the active source-faithful Suzuki odd-sector positivity audit.

The active finite-high block is

\[
F=\{22,24,\dots,4000\},
\]

with source-faithful prime/power support in

\[
q\in\{2,3,4,5,7\},
\]

as implemented in

`research-notes/suzuki_odd_M4000_finite_high_midpoint_replay.py`.

The relevant source sequence is

\[
A_n^{\rm prime}
=\sum_q \frac{\Lambda(q)}{\sqrt q}
\sin\!\left(\frac{n\pi\log q}{2}\right),
\qquad
Z_n^{\rm prime}=2A_n^{\rm prime}.
\]

This checkpoint asks whether resolving that arithmetic source through the four real mod-12 characters

\[
1,\qquad \chi_{-4},\qquad \chi_{-3},\qquad \chi_{12}
\]

creates useful cancellation in either the finite-high source block or the `F <-> T` cross term.

## 1. Direct character action on the Suzuki mode index is unavailable [D/Audit]

The odd Suzuki finite-high block uses only even Fourier indices

\[
n=22,24,\dots,4000.
\]

Every primitive real Dirichlet character modulo 12 vanishes on even integers. Therefore a literal weighting

\[
A_{mn}\mapsto \chi(n)A_{mn}
\]

or `chi(m) chi(n)` decomposition on the original matrix indices is identically zero and carries no V4 information.

**[Audit]** Reindexing by `n/2` would create nonzero residue classes, but no such reindexing is presently justified by the source-faithful Suzuki derivation as a mod-12 arithmetic carrier. It is therefore not used here.

The legitimate mod-12 resolution is instead on the arithmetic prime/power source index `q`.

## 2. Exact character table on the actual source support [D]

For the source support

\[
q\in\{2,3,4,5,7\},
\]

the four real mod-12 characters give

\[
\begin{array}{c|rrrr}
q & 1^\times & \chi_{-4} & \chi_{-3} & \chi_{12}\\
\hline
2 & 0&0&0&0\\
3 & 0&0&0&0\\
4 & 0&0&0&0\\
5 & 1&1&-1&-1\\
7 & 1&-1&1&-1
\end{array}
\]

Hence the ramified/nonunit source pieces `q=2,3,4` are annihilated by all four unit characters, while the unit-supported source has only two independent combinations.

If `S_5` and `S_7` denote the source operators generated by the `q=5` and `q=7` terms, then exactly

\[
\boxed{
S_{1^\times}=S_5+S_7,
}
\]

\[
\boxed{
S_{\chi_{-4}}=S_5-S_7,
}
\]

\[
\boxed{
S_{\chi_{-3}}=-S_5+S_7=-S_{\chi_{-4}},
}
\]

and, crucially,

\[
\boxed{
S_{\chi_{12}}=-S_5-S_7=-S_{1^\times}.
}
\]

Therefore

\[
\boxed{
\|S_{\chi_{12}}\|=\|S_{1^\times}\|
}
\]

for every sign-invariant operator norm.

This is an exact obstruction to obtaining a sharper source norm merely by passing from the unit-principal channel to `chi_12` in the present compact-support Suzuki source.

## 3. Finite-high numerical diagnostic [N]

Using the source-faithful formulas of the odd `M=4000` midpoint replay, the individual `q=5` and `q=7` finite-high source matrices were assembled on

\[
F=\{22,24,\dots,4000\}.
\]

Their Frobenius norms are

\[
\|S_5\|_F\approx 20.04638743,
\qquad
\|S_7\|_F\approx 7.59346587.
\]

For the two independent character combinations,

\[
\|S_5+S_7\|_F\approx21.44047523,
\]

\[
\|S_5-S_7\|_F\approx21.43228331.
\]

Thus the `chi_12` finite-high source Frobenius norm is exactly the first of these values because it differs only by an overall sign:

\[
\boxed{
\|S_{\chi_{12}}\|_F
=\|S_5+S_7\|_F
\approx21.44047523.
}
\]

A power-iteration diagnostic for the spectral norm gave approximately

\[
\|S_5+S_7\|_2\approx1.176,
\]

with the same value for `chi_12` by exact sign symmetry.

**[Audit]** These values are midpoint diagnostics only; they are not outward interval bounds.

## 4. Explicit near-cross numerical diagnostic [N]

For the explicit near cross

\[
F=\{22,24,\dots,4000\},
\qquad
T_{\rm near}=\{4002,4004,\dots,16000\},
\]

the same source-faithful off-diagonal formula was evaluated separately for `q=5` and `q=7`.

The Frobenius norms are

\[
\|C_5\|_F\approx0.69286845,
\qquad
\|C_7\|_F\approx0.65190057.
\]

For the unit-principal / `chi_12` combination,

\[
\boxed{
\|C_5+C_7\|_F
=\|C_{\chi_{12}}\|_F
\approx0.92765364.
}
\]

A direct two-sided power iteration gives the midpoint spectral-norm diagnostics

\[
\|C_5\|_2\approx0.45982,
\qquad
\|C_7\|_2\approx0.48233,
\]

\[
\boxed{
\|C_5+C_7\|_2
=\|C_{\chi_{12}}\|_2
\approx0.64717.
}
\]

For comparison, the historical full-cross transcript records

\[
0.9772519211742844
\]

for the combined low-rank cross diagnostic and the outward target

\[
\|A_{FT}\|<1.015.
\]

Thus the unit-supported `chi_12` prime channel is already a substantial fraction of the observed cross scale. It is not a small remainder exposed by character cancellation.

## 5. Why `chi_12` does not sharpen the active proof constants [D/N]

The new cone/Pell work shows that `chi_12` has a genuine exact role as

1. the V4 shell-displacement character;
2. the sign character for Pell-time orientation;
3. the real-quadratic ideal-counting character for `Q(sqrt(3))`.

However, on the specific compact arithmetic support of the current Suzuki source,

\[
\chi_{12}(5)=\chi_{12}(7)=-1.
\]

So `chi_12` does not distinguish the two unit source atoms that survive the support cutoff. It reverses both together.

Consequently:

- it does **not** reduce the finite-high source norm;
- it does **not** reduce the explicit near-cross source norm;
- it does **not** address the ramified/nonunit `q=2,3,4` source pieces, since all unit characters vanish there;
- it therefore does **not** presently improve the required outward certification of
  \[
  A_{FF}\succeq0.53I
  \]
  or the cross target
  \[
  \|A_{FT}\|<1.015.
  \]

## 6. The channels that actually contain 5-vs-7 cancellation [D/N]

The only nontrivial sign separation on the present unit support is

\[
S_5-S_7,
\]

carried by `chi_-4` and, with opposite sign, `chi_-3`.

Numerically this does not produce a dramatic norm reduction either. On the finite-high block,

\[
\|S_5-S_7\|_F\approx21.43228,
\]

nearly the same as

\[
\|S_5+S_7\|_F\approx21.44048.
\]

For the explicit near cross, a two-sided power iteration gives

\[
\|C_5-C_7\|_2\approx0.69788,
\]

which is actually somewhat larger than the `chi_12` / unit-principal value

\[
\approx0.64717.
\]

So none of the four mod-12 character combinations supplies a material norm improvement at this stage.

## 7. Consequence for the positivity program [Audit]

The V4 / `Q(sqrt3)` bridge remains structurally real, but the present character projection does not solve the active positivity bottleneck.

The correct next move remains the direct outward source/arithmetic enclosure for the full finite-high matrix, together with independent regeneration of the cross estimate.

The new character analysis should nevertheless remain in the audit because it rules out a tempting but ineffective shortcut and identifies the precise reason:

\[
\boxed{
\text{the current Suzuki unit source support is only }\{5,7\},
\quad
\chi_{12}|_{\{5,7\}}\equiv-1.
}
\]

A `chi_12` cancellation could become relevant only in a source representation containing unit residue classes from both sign cosets

\[
\{1,11\}
\quad\text{and}\quad
\{5,7\},
\]

which the present compact-support prime/power sum does not.

---

**Checkpoint conclusion.** The source-faithful Suzuki matrix can be legitimately resolved by mod-12 characters only on its arithmetic `q` support, not directly on the even Fourier indices. On the actual support `q={2,3,4,5,7}`, the four V4 channels collapse to two independent unit combinations, and

\[
\boxed{S_{\chi_{12}}=-S_{1^\times}.}
\]

Therefore the `chi_12` channel yields no material sharpening of either the finite-high source norm or the explicit near-cross norm. The cone / `Q(sqrt3)` / `chi_12` connection remains structurally significant, but it is not the missing positivity estimate.