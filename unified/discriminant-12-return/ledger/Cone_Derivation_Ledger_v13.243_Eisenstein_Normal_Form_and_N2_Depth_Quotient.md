# Cone Derivation Ledger v13.243 — Eisenstein Normal Form and N^2 Depth Quotient

Date: 2026-09-05
Status: EXACT NEW RESULT — continuation of v13.242; research branch remains open

## 0. Pre-write synchronization

Immediately before this write, the authoritative project README and current `master` tip were re-fetched. The tip remained

`c5af2c6bf3730b73b07b142c5ee3f2efc50bc90e`

with v13.242 as the highest ledger checkpoint. No newer external-audit checkpoint had landed. This entry extends the reconciled research branch and does not revise the audited principal v0.3.5 paper.

## 1. Question

v13.242 proved that every q=7 support operator preserves the same primitive rank-two signal lattice and that the second determinantal divisor is the exact determinant cocycle

\[
\Delta_2=3^a4^b.
\]

The natural next question is whether the apparently different restricted 2x2 matrices have a small multiplication law and a canonical normal form.

They do. In fact the entire noncollapsed restricted support semigroup is commutative, generated by one sixth-order unit, one norm-3 element, and the central scalar 2. The resulting ring is the Eisenstein integer ring `Z[zeta_6]`, and modulo decorated-fiber phase equivalence the noncollapsed orbit is exactly the lattice `N^2`.

## 2. Three generators

From the v13.242 table define

\[
U:=R_{2,-}=R_{5,-}
=
\begin{pmatrix}
1&-1\\
1&0
\end{pmatrix},
\]

\[
N:=R_{2,+}=R_{5,+}
=
\begin{pmatrix}
-1&-1\\
1&-2
\end{pmatrix},
\]

and

\[
S:=R_{1,-}=R_{6,-}=2I.
\]

The remaining nonzero local matrices are

\[
R_{3,-}=R_{4,-}=U^{-1},
\]

and

\[
R_{3,+}=R_{4,+}=UN.
\]

The two collapsed letters are

\[
R_{1,+}=R_{6,+}=0.
\]

Thus every q=7 local support letter is one of

\[
0,\quad 2I,\quad U^{\pm1},\quad N,\quad UN.
\]

## 3. Sixth-root relation

Direct multiplication gives

\[
U^2-U+I=0.
\]

Therefore

\[
\boxed{U^6=I,}
\]

and

\[
\boxed{U^{-1}=I-U.}
\]

So `U` is an integral matrix realization of a primitive sixth root of unity.

## 4. The norm-3 generator is a polynomial in U

A direct subtraction gives the unexpectedly simple identity

\[
\boxed{N=U-2I.}
\]

Hence `N` automatically commutes with `U`. Consequently every nonzero restricted support operator commutes with every other one, since `S=2I` is central.

This proves

\[
\boxed{\text{the noncollapsed q=7 restricted support semigroup is commutative.}}
\]

The second determinant-three matrix is then simply

\[
UN=U(U-2I).
\]

Using `U^2=U-I`,

\[
UN=-U-I,
\]

which agrees exactly with the v13.242 matrix

\[
\begin{pmatrix}
-2&1\\
-1&-1
\end{pmatrix}.
\]

## 5. Fundamental quadratic relation for N

Since

\[
N=U-2I,
\]

and `U^2-U+I=0`, one obtains

\[
N^2
=(U-2I)^2
=U^2-4U+4I
=3(I-U).
\]

But `I-U=U^{-1}`. Therefore

\[
\boxed{N^2=3U^{-1}.}
\]

Equivalently,

\[
\boxed{N^2U=3I.}
\]

The characteristic polynomial of `N` is correspondingly

\[
\boxed{x^2+3x+3.}
\]

Its determinant is 3 and its trace is -3.

This one relation explains the determinant-three depth algebra: two norm-3 steps combine to a scalar factor 3 together with one inverse phase step.

## 6. Eisenstein integer realization

Let `zeta_6` be a primitive sixth root of unity satisfying

\[
zeta_6^2-zeta_6+1=0.
\]

The assignment

\[
\boxed{zeta_6\longmapsto U}
\]

extends to a ring isomorphism from

\[
\mathbf Z[zeta_6]
\]

onto the integer matrix ring generated by `U`.

This is the Eisenstein integer ring (equivalently `Z[omega]`, with `omega=zeta_6^2`). Under this realization,

\[
U\leftrightarrow zeta_6,
\qquad
N\leftrightarrow zeta_6-2,
\qquad
S\leftrightarrow2.
\]

For any element `a+b zeta_6`, the determinant of the corresponding 2x2 matrix is the quadratic field norm

\[
\boxed{
\det(aI+bU)
=N_{\mathbf Q(\sqrt{-3})/\mathbf Q}(a+bzeta_6).
}
\]

In particular,

\[
N(zeta_6)=1,
\]

\[
N(zeta_6-2)=3,
\]

and

\[
N(2)=4.
\]

Thus the determinant alphabet of v13.242 is exactly an Eisenstein norm alphabet.

Guardrail: this emergent `Q(sqrt(-3))` operator-plane arithmetic is not the principal paper's real quadratic field `Q(sqrt(3))`. They are distinct structures arising at different levels of the project.

## 7. Exact normal form for every noncollapsed word

Let a noncollapsed support word contain:

- `a` determinant-three letters, each equal to `N` or `UN`;
- `b` determinant-four letters, each equal to `S=2I`;
- any number of preserving letters `U` or `U^{-1}`.

Because all nonzero generators commute, every word can be reordered exactly into

\[
R_w=S^bN^aU^j
\]

for some `j mod 6`. Therefore

\[
\boxed{R_w=2^bN^aU^j.}
\]

This is already a canonical three-coordinate form `(a,b,j)`.

Using

\[
N^2=3U^{-1},
\]

write

\[
a=2m+\epsilon,
\qquad \epsilon\in\{0,1\}.
\]

Then

\[
N^a=(N^2)^mN^\epsilon
=3^mU^{-m}N^\epsilon.
\]

Hence a second exact normal form is

\[
\boxed{
R_w
=2^b3^mN^\epsilon U^{j-m},
\qquad
\epsilon\in\{0,1\},
\quad j\in\mathbf Z/6\mathbf Z.
}
\]

So after extracting scalar arithmetic depth, only one binary norm-3 parity and one sixth-order phase remain.

## 8. Uniqueness of the normal form

Suppose

\[
2^bN^aU^j
=2^{b'}N^{a'}U^{j'}.
\]

Taking determinants gives

\[
4^b3^a=4^{b'}3^{a'}.
\]

Unique factorization in the rational integers forces

\[
\boxed{a=a',\qquad b=b'.}
\]

Because `N` is invertible over `Q` and `U` is invertible over `Z`, cancellation then gives

\[
U^j=U^{j'}.
\]

Since `U` has exact order 6,

\[
\boxed{j\equiv j'\pmod6.}
\]

Thus

\[
\boxed{
R_w=2^bN^aU^j
}
\]

is a unique exact normal form for every noncollapsed restricted support word.

The zero letters form a separate absorbing state.

## 9. Exact order-independence theorem

Because the restricted semigroup is commutative, the order in which support labels occur is irrelevant on the q=7 prototype plane.

Only three pieces of data matter:

1. the total number `a` of determinant-three letters;
2. the total number `b` of determinant-four letters;
3. the net sixth-order phase exponent `j` contributed by the choices `N` versus `UN` and `U` versus `U^{-1}`.

Therefore

\[
\boxed{
\text{two noncollapsed support words induce the same restricted matrix}
\iff
(a,b,j)=(a',b',j'\bmod6).
}
\]

This is much stronger than the determinant cocycle alone.

## 10. Phase action at every arithmetic depth

From v13.240, `U` acts on the prototype decorated fiber only by allowed cofactor rephasing and global sign. Since `U` commutes with `N` and `S`, the same remains true after any arithmetic-depth operator:

\[
U^jS^bN^a\Phi_*
=S^bN^aU^j\Phi_*.
\]

Thus changing `j` moves only inside the same decorated-fiber equivalence class at fixed `(a,b)`.

So every noncollapsed arithmetic depth carries the same sixth-order phase action inherited from the recurrent sector.

In this precise sense the full noncollapsed orbit is a `C_6` phase fiber over an arithmetic depth lattice.

## 11. Exact quotient by decorated-fiber equivalence

Let `[\Phi]` denote the equivalence class under multiplicative cofactor relabelling and global sign from v13.239.

Every noncollapsed state has the form

\[
2^bN^aU^j\Phi_*.
\]

The phase factor `U^j` disappears in the equivalence quotient, so

\[
[2^bN^aU^j\Phi_*]
=[2^bN^a\Phi_*].
\]

Conversely, if

\[
[2^bN^a\Phi_*]
=[2^{b'}N^{a'}\Phi_*],
\]

then the second determinantal divisor must agree. By v13.242,

\[
3^a4^b=3^{a'}4^{b'}.
\]

Hence

\[
a=a',\qquad b=b'.
\]

Therefore the noncollapsed decorated-fiber quotient is classified exactly by

\[
\boxed{(a,b)\in\mathbf N_0^2.}
\]

Equivalently,

\[
\boxed{
\{\text{noncollapsed q=7 orbit classes}\}
\cong\mathbf N_0^2.
}
\]

The collapsed rank-zero state is one additional absorbing point.

## 12. Transition law on the N^2 quotient

The twelve local support labels descend to an extremely simple quotient dynamics.

For the four preserving letters

\[
(r,-1),\qquad r\in\{2,3,4,5\},
\]

\[
\boxed{(a,b)\mapsto(a,b).}
\]

For the four determinant-three letters

\[
(r,+1),\qquad r\in\{2,3,4,5\},
\]

\[
\boxed{(a,b)\mapsto(a+1,b).}
\]

For the two determinant-four letters

\[
(r,-1),\qquad r=\pm1,
\]

\[
\boxed{(a,b)\mapsto(a,b+1).}
\]

For the two determinant-zero letters

\[
(r,+1),\qquad r=\pm1,
\]

the orbit enters the absorbing collapsed state.

Thus the quotient graph is exactly

\[
\boxed{
\mathbf N_0^2
\quad\text{with unit steps in the two coordinate directions,}
}
\]

plus one absorbing zero state.

## 13. Arithmetic interpretation of the two depth coordinates

In the Eisenstein ring, the element

\[
N\leftrightarrow zeta_6-2
\]

has norm 3. It is therefore associated with the unique ramified prime above 3 in `Q(sqrt(-3))`.

The scalar

\[
S\leftrightarrow2
\]

has norm 4; the rational prime 2 is inert in `Q(sqrt(-3))`.

Accordingly the coordinates `(a,b)` can be read as the exponents of two distinguished Eisenstein prime directions generated by the norm-3 and norm-4 support operators.

The determinant divisor records exactly

\[
\boxed{\Delta_2=3^a4^b.}
\]

So the `N^2` depth lattice is not merely combinatorial: it is the valuation cone of the restricted support semigroup in the Eisenstein operator ring.

## 14. Final structural picture

The q=7 prototype support dynamics now admits the exact decomposition

\[
\boxed{
\text{restricted noncollapsed operator semigroup}
\cong
\{2^bN^aU^j:
 a,b\ge0,
 j\in\mathbf Z/6\mathbf Z\},
}
\]

with

\[
U^6=I,
\qquad
N=U-2I,
\qquad
N^2=3U^{-1},
\qquad
UN=NU.
\]

At the decorated-fiber level this becomes

\[
\boxed{
C_6\text{ phase action over }\mathbf N_0^2\text{ arithmetic depth},
}
\]

and after quotienting by the allowed phase equivalence it becomes exactly

\[
\boxed{\mathbf N_0^2}
\]

plus the absorbing collapse state.

This realizes precisely the structure anticipated after v13.242: a finite phase object sitting over a rigid infinite two-dimensional arithmetic depth lattice.

## 15. Guardrails

1. All statements here concern the exact q=7 rank-two prototype operator representation derived in v13.242.
2. The emergent Eisenstein field `Q(sqrt(-3))` is not identified with the principal real quadratic field `Q(sqrt(3))`.
3. The `N^2` classification is for the noncollapsed operator orbit generated from the q=7 prototype under the complete twelve-letter local alphabet.
4. The actual sets of support primes remain unbounded and retain arithmetic data discarded by the operator quotient.
5. A determinant-zero letter annihilates the prototype plane and is represented by a separate absorbing state.
6. The audited principal v0.3.5 theorem package is unchanged.

## 16. Next task

The next natural question is why the rank-two support representation lands in the Eisenstein ring so cleanly. There are two exact directions to test:

1. derive `U^2-U+I=0` and the Eisenstein norm directly from the q=7 centered Legendre/Gauss-sum structure rather than from the explicit matrices;
2. determine whether the `q=5` prototype plane admits an analogous quadratic-order representation and compare its ring discriminant, unit group, and support-depth semigroup with the q=7 Eisenstein case.

That comparison may explain structurally why q=7 supports a nontrivial recurrent sector while q=5 does not.